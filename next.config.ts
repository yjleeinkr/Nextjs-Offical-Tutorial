import type { NextConfig } from 'next';

const nextConfig: NextConfig = {
	/* config options here */
	// experimental: {
	// 	ppr: 'incremental', // 특정 라우트들에서 PPR (Partial PreRendering) 가능해짐
	// },
};

export default nextConfig;
